# 🧪 Тестові сценарії API (Test Cases)

**Проєкт:** Code Mastery

**Об'єкт тестування:** API (Backend)

**Інструмент:** Postman

Цей документ містить результати тестування основних ендпоінтів системи: реєстрації, авторизації, роботи з курсами та уроками.

## 📋 Зведення результатів

| ID  | Назва сценарію | Метод | Статус |
| --- | --- | --- | --- |
| **TC-01** | Реєстрація нового користувача | `POST` | ✅ PASSED |
| **TC-02** | Авторизація користувача | `POST` | ✅ PASSED |
| **TC-03** | Отримання інформації про курс | `GET` | ✅ PASSED |
| **TC-04** | Список курсів (фільтрація) | `GET` | ✅ PASSED |
| **TC-05** | Перегляд уроку | `GET` | ✅ PASSED |

## 🔹 TC-01: Реєстрація нового користувача

-   **Опис:** Перевірка можливості створення нового облікового запису.
    
-   **URL:** `/sign-up`
    
-   **Метод:** `POST`
    

| Етап | Деталі |
| --- | --- |
| **Вхідні дані (Request)** | **JSON Body:** `{ "email": "test@gmail.com", "password": "12345678Aa", "first_name": "John", "last_name": "Doe", "role": "student" }` |
| **Очікуваний результат** | **Status:** `201 Created` **Body:** Повідомлення про успішну реєстрацію. |
| **Фактичний результат** | **Status:** `201 Created` **Body:** `{ "message": "User registered successfully" }` |
| **Висновок** | ✅ **PASSED** |

## 🔹 TC-02: Авторизація користувача

-   **Опис:** Перевірка входу в систему з валідними даними.
    
-   **URL:** `/sign-in`
    
-   **Метод:** `POST`
    

| Етап | Деталі |
| --- | --- |
| **Вхідні дані (Request)** | **JSON Body:** `{ "email": "test@gmail.com", "password": "12345678Aa" }` |
| **Очікуваний результат** | **Status:** `201 Created` **Body:** Токен доступу або повідомлення про успіх. |
| **Фактичний результат** | **Status:** `201 Created` **Body:** `{ "message": "Logged in successfully" }` |
| **Висновок** | ✅ **PASSED** |

## 🔹 TC-03: Отримання інформації про курс

-   **Опис:** Отримання повної інформації про курс за ID.
    
-   **URL:** `/course/34`
    
-   **Метод:** `GET`
    

| Етап | Деталі |
| --- | --- |
| **Вхідні дані (Request)** | _Параметри в URL (Path params)_ |
| **Очікуваний результат** | **Status:** `200 OK` **Body:** Об'єкт курсу з полями `id`, `name`, `author`, `lessons`. |
| **Фактичний результат** | **Status:** `200 OK` **Body:** `{ "course": { "course_id": 34, "name": "Java Fundamentals", "author": { "first_name": "Name" }, ... } }` |
| **Висновок** | ✅ **PASSED** |

## 🔹 TC-04: Отримання списку курсів

-   **Опис:** Перевірка пагінації та фільтрації списку курсів.
    
-   **URL:** `/courses?limit=10&skip=0&levels=Beginner`
    
-   **Метод:** `GET`
    

| Етап | Деталі |
| --- | --- |
| **Вхідні дані (Request)** | _Query Params:_ `limit=10`, `levels=Beginner` |
| **Очікуваний результат** | **Status:** `200 OK` **Body:** Масив курсів та загальна кількість (`count`). |
| **Фактичний результат** | **Status:** `200 OK` **Body:** `{ "data": [{ "name": "Intro to JS", "level": "Beginner" }], "count": 2 }` |
| **Висновок** | ✅ **PASSED** |

## 🔹 TC-05: Перегляд окремого уроку

-   **Опис:** Отримання контенту конкретного уроку.
    
-   **URL:** `/courses/34/1`
    
-   **Метод:** `GET`
    

| Етап | Деталі |
| --- | --- |
| **Вхідні дані (Request)** | _Path Params:_ `courseId=34`, `lessonId=1` |
| **Очікуваний результат** | **Status:** `200 OK` **Body:** Об'єкт уроку з контентом та вкладеннями. |
| **Фактичний результат** | **Status:** `200 OK` **Body:** `{ "lesson": { "title": "Variables", "content": "Video...", "course": { "id": 34 } } }` |
| **Висновок** | ✅ **PASSED** |